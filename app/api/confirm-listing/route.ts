import {NextResponse} from 'next/server'; import {createServerSupabaseClient} from '@/lib/supabase-server';
export async function POST(req:Request){const sb=await createServerSupabaseClient();const {data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required.'},{status:401});const b=await req.json();const {data,error}=await sb.from('listings').update({last_confirmed_at:new Date().toISOString(),verification_status:'owner_confirmed',updated_at:new Date().toISOString()}).eq('id',b.id).eq('owner_id',user.id).select().single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({listing:data});}
