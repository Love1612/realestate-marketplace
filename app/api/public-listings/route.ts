import {NextResponse} from "next/server"; import {createServerSupabaseClient} from "@/lib/supabase-server";
export async function GET(){const sb=await createServerSupabaseClient();const {data,error}=await sb.from("listings").select("id,title,property_type,address,city,state,zip,monthly_rent,bedrooms,bathrooms,latitude,longitude,listing_photos(id,public_url,sort_order)").eq("status","live").gt("expires_at",new Date().toISOString()).order("created_at",{ascending:false}).limit(500);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({listings:data||[]},{headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300"}})}
