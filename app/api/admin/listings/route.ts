import {NextResponse} from 'next/server'; import {requireAdmin} from '@/lib/admin';
export async function GET(){const {admin,isAdmin}=await requireAdmin();if(!isAdmin||!admin)return NextResponse.json({error:'Forbidden'},{status:403});const {data,error}=await admin.from('listings').select('id,title,owner_id,status,monthly_rent,city,state,created_at,expires_at').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({listings:data||[]});}
export async function PATCH(req:Request){const {admin,isAdmin}=await requireAdmin();if(!isAdmin||!admin)return NextResponse.json({error:'Forbidden'},{status:403});const {id,status}=await req.json();if(!id||!['live','paused','expired'].includes(status))return NextResponse.json({error:'Invalid listing status.'},{status:400});const {error}=await admin.from('listings').update({status}).eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
