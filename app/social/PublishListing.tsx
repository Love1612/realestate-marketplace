"use client";
import {useState} from 'react';
export default function PublishListing({listingId}:{listingId:string}){const [status,setStatus]=useState(''); const providers=['facebook','linkedin','x']; async function publish(p:string){setStatus(`Publishing to ${p}…`); const r=await fetch(`/api/social/${p}/publish`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({listingId})}); const d=await r.json(); setStatus(r.ok?`Published to ${p}.`:`${p}: ${d.error||'Publish failed.'}`)} return <div className="social-publish"><strong>Promote this listing</strong><div className="toolbar">{providers.map(p=><button className="btn btn-light" key={p} onClick={()=>publish(p)}>Post to {p==='x'?'X':p[0].toUpperCase()+p.slice(1)}</button>)}</div>{status&&<span className="muted">{status}</span>}</div>}
