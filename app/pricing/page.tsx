import Link from 'next/link'; import {LISTING_PLANS,PORTFOLIO_TIERS} from '@/lib/pricing';
export default function Pricing(){return <main className="section"><div className="container"><h1>Simple, competitive pricing</h1><p className="muted">Pay for visibility and active advertised vacancies—not every unit you own.</p><h2 style={{marginTop:30}}>Single listings</h2><div className="grid">{Object.entries(LISTING_PLANS).map(([k,p])=><div className="card" key={k}><div className="card-body"><span className="badge">{p.name}</span><h3>{p.label}</h3><p className="muted">{k==='basic'?'Standard listing placement.':k==='featured'?'Higher visibility and featured placement.':'Highest visibility with rich-media emphasis.'}</p><Link className="btn btn-primary" href={`/listings/new?plan=${k}`}>Choose {p.name}</Link></div></div>)}</div><h2 style={{marginTop:36}}>Portfolio tiers</h2><div className="grid">{PORTFOLIO_TIERS.map(t=><div className="card" key={t.tier}><div className="card-body"><h3>{t.listings}+ active listings</h3><div className="price">{t.price===null?'Custom':`$${t.price}/mo`}</div><p className="muted">Dashboard, communities, bulk tools, staff access, analytics, CSV import/export and syndication-ready feeds.</p><Link className="btn btn-secondary" href="/portfolio">Manage portfolio</Link></div></div>)}</div></div></main>}
