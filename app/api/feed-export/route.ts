import {NextResponse} from 'next/server'; import {createServerSupabaseClient} from '@/lib/supabase-server';
export async function GET(){const sb=await createServerSupabaseClient();const {data:{user}}=await sb.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required.'},{status:401});const {data}=await sb.from('listings').select('id,title,description,property_type,address,city,state,zip,monthly_rent,bedrooms,bathrooms,available_on,latitude,longitude,verification_status,last_confirmed_at,listing_photos(public_url)').eq('owner_id',user.id).eq('status','live');return NextResponse.json({feed_version:'renthub-v12',generated_at:new Date().toISOString(),listings:data||[]});}
