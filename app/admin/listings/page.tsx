'use client'; import {useEffect,useState} from 'react';
export default function AdminListings(){const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);async function load(){const r=await fetch('/api/admin/listings');const d=await r.json();setItems(d.listings||[]);setLoading(false);}useEffect(()=>{load()},[]);async function setStatus(id:string,status:string){await fetch('/api/admin/listings',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status})});load();}return <main className="section"><div className="container"><h1>Manage listings</h1>{loading?<p>Loading…</p>:<div style={{display:'grid',gap:12}}>{items.map(l=><div className="panel" key={l.id}><strong>{l.title}</strong><p className="muted">{l.city}, {l.state} · ${Number(l.monthly_rent).toLocaleString()}/mo · {l.status}</p><div style={{display:'flex',gap:8,flexWrap:'wrap'}}>{['live','paused','expired'].map(s=><button className="btn btn-light" key={s} onClick={()=>setStatus(l.id,s)}>{s}</button>)}</div></div>)}</div>}</div></main>}
